syntax = "proto3";

import "domain/Entity.proto";
package com.crd.alpha.edm.domain;


message TestEntity {
  string name = 1;
  int64 primaryKey = 2;
  string this_was_here = 3;
  int64 so_was_i = 4;
  repeated KevValuePair kvp = 5;
}

message TestEntities {
  repeated TestEntity testEntity  = 1;
}

message TestPrimaryKey{
  int64 primaryKey = 1;
}

message TestPrimaryKeys{
  repeated int64 primaryKey = 1;
}